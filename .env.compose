# Compose-specific environment file
# Do NOT commit secrets to public repos. Replace placeholders with real values on server.
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Taepryung
DB_NAME=country_data
APP_PORT=8080
